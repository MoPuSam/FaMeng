# 数据库的配置
spring.datasource.url=jdbc:mysql://192.168.88.199:3307/latoapp?useUnicode=true&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=IfjWVwHB
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.max-idle=10
spring.datasource.max-wait=10000
spring.datasource.min-idle=5
spring.datasource.initial-size=5
spring.datasource.dbcp.max-active=10
server.port=8888
server.session.timeout=10
server.tomcat.uri-encoding=UTF-8
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
# 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙
spring.datasource.filters=stat,wall,log4j
# 该配置节点为独立的节点，有很多同学容易将这个配置放在spring的节点下，导致配置无法被识别
#注意：一定要对应mapper映射xml文件的所在路径
mybatis.mapper-locations= classpath:mapping/*.xml
# 注意：对应实体类的路径
mybatis.type-aliases-package= com.oulam.bean

#pagehelper分页插件
pagehelper.helperDialect= mysql
pagehelper.reasonable=true
pagehelper.supportMethodsArguments=true
pagehelper.params= count=countSql

#配置模板引擎
spring.thymeleaf.prefix=classpath:/form-1/
#防止中文乱码
spring.http.encoding.charset=UTF-8
spring.http.encoding.force=true
spring.http.encoding.enabled=true
#设置上传文件大小限制
spring.http.multipart.enabled=true
spring.http.multipart.max-file-size=6MB
spring.http.multipart.max-request-size=10MB
#文件后台路径
file.path = G:/upload/

